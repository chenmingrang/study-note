并行 （与并发不一样）
    --将一件工作分成很多块，分别由不同的进程来执行，最后将结果合并。


并行的应用场景
   --olap（在线分析）数据仓库

   --整块的数据读取操作
       --FTS:（全表扫描）full table scan
       --IFFS:index fast full  san

   --并行执行高效的要素
       --充足的系统资源
       --待处理的数据分布均匀






















